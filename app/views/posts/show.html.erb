<p id="notice"><%= notice %></p>

<h1><%= @post.title%></h1>

<h3><%= @post.body %></h3>

<%= render @post.comments %>

<%= form_for [@post, @post.comments.build] do |f| %>
	<div class='field'>
		<%= f.text_area :body%>
	</div>
	<div class='actions'>
		<%= f.submit %>
	</div>
<%end%>
